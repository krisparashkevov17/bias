# Thesis analysis pipeline — Python 3.9
# Inputs and the FA_Functions helpers come from the LWOW repository
# (Abramski et al., 2025): https://github.com/LLMWorldOfWords/LWOW
numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
statsmodels==0.14.6
networkx==3.2.1
igraph==1.0.0
matplotlib==3.9.4
seaborn==0.13.2
scikit-learn==1.6.1
tqdm==4.67.3

# Required: utils.py imports helpers from LWOW's FA_Functions.py, which
# loads nltk at module level. Pin 3.8.1 — NLTK >= 3.9 changes the Humans
# network slightly if the LWOW preprocessing stage is re-run.
nltk==3.8.1
